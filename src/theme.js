import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: { main: '#6a1b9a' }, // purple
        secondary: { main: '#9c27b0' }, // lighter purple
        background: { default: '#fefefe' }, // white
    },
});

export default theme;
